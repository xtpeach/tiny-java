server:
  port: 30001
  tomcat:
    accept-count: 2000
# spring
spring:
  #application
  application:
    name: ${APPLICATION_NAME:minima}
  datasource:
    url: jdbc:sqlite:/home/data/database.db
    driver-class-name: org.sqlite.JDBC